server:
  port: 80

spring:
  datasource:
    url: ${SRING_DATASOURCE_URL}
    username: ${SPRINT_DATASOURCE_USERNAME}
    password: ${SPRINT_DATASOURCE_PASSWORD}
  jpa:
    properties:
      hibernate:
        format_sql: false
  boot:
    admin:
      client:
        url: ${SPRING_BOOT_ADMIN_CLIENT_URL}

logging:
  level:
    com.sprint.mission.discodeit: info
    org.hibernate.SQL: info

management:
  endpoint:
    health:
      show-details: never
  info:
    env:
      enabled: false
