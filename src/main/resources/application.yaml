spring:
  application:
    name: discodeit
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 30MB
  data:
    web:
      pageable:
        default-page-size: 50


discodeit:
  repository:
    type: file
    file-directory: .discodeit