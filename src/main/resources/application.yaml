spring.application.name: discodeit

discodeit:
  repository:
    type: file
    file:
      user: users.ser
      channel: channels.ser
      message: messages.ser
      binaryContent: binaryContents.ser
      readStatus: readStatuses.ser
      userStatus: userStatuses.ser

