spring:
  application:
    newName:discodeit

discodeit:
  repository:
    type: file   # jcf | file
    file-directory: .discodeit

server:
  port: 8080

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
