spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/discodeit
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  server:
    port: 8080

logging:
  level:
    root: INFO
    com.sprint.mission.discodeit.service.basic: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace