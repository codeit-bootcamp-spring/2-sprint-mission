spring:
  #profiles: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 8080

logging:
  level:
    com.sprint.mission.discodeit: debug
