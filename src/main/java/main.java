import com.sprint.mission.discodeit.entity.Channel;
import com.sprint.mission.discodeit.jcf.JCFChannel;
import com.sprint.mission.discodeit.service.ChannelService;
import java.util.UUID;

public class main {
    public static void main(String[] args) {
//        UUID uuid = UUID.randomUUID();
//        long millis = System.currentTimeMillis();
//        Channel channel = new Channel(uuid, millis);
//
//        UUID uuid2 = UUID.randomUUID();
//        long millis2 = System.currentTimeMillis();
//        Channel channel1 = new Channel(uuid2, millis2);
//
//        ChannelService channelService = new JCFChannel(
//        channelService.create(channel);
//        channelService.create(channel1);
//
//        channelService.findAll().forEach(System.out::println);
//        System.out.println("--------");
//        channelService.update(channel.getId());
//        System.out.println("--------");
//        channelService.findAll().forEach(System.out::println);
    }
}
