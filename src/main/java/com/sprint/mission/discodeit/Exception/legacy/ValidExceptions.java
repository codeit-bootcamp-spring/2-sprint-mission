package com.sprint.mission.discodeit.Exception.legacy;

public class ValidExceptions {
    public static ValidException DUPLICATE_USER = new ValidException(CommonCode.DUPLICATE_USER);
    public static ValidException DUPLICATE_USER_STATUS = new ValidException(CommonCode.DUPLICATE_USER_STATUS);
    public static ValidException DUPLICATE_READ_STATUS = new ValidException(CommonCode.DUPLICATE_READ_STATUS);
    public static ValidException INVALID_PASSWORD = new ValidException(CommonCode.INVALID_PASSWORD);
}
